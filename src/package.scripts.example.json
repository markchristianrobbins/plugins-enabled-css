{
  "scripts": {
    "build": "esbuild main.ts --bundle --platform=node --format=cjs --target=es2020 --outfile=main.js --external:obsidian --external:electron --sourcemap",
    "dev": "npm run build -- --watch",
    "check": "tsc --noEmit"
  }
}